version: "3"

services:
  test:
    build:
      context: ../../../..
      dockerfile: minotoring/tests/integration_tests/packaging/Dockerfile
    command: minotoring
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "3000:8888"

