version: "3"

services:
  app:
    build: .
    command: python run.py
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8888:8888"
    volumes:
      - .:/code

  npm:
    build: front
    command: npm start
    ports:
      - "3000:3000"
    volumes:
      - ./front:/code
